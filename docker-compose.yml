services:
  db:
    image: mysql
    container_name: mysqldatabase
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - mysqldata:/var/lib/mysql
      - mysqlconfig:/etc/mysql/my.cnf

volumes:
  mysqlconfig:
  mysqldata:

