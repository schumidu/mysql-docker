services:
  db:
    image: mysql
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - mysqldata:/var/lib/mysql
      - mysqlconfig:/etc/mysql/my.cnf

    environment:
      - MYSQL_ROOT_PASSWORD=bt7LaWX773n2fdHp

volumes:
  mysqlconfig:
  mysqldata:

